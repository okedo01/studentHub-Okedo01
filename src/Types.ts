export interface Courses {
    id: number
    title: string
    description: string
    duration: string
    category: string
    experience: string
}

export interface Students {
    id: number
    name: string
    email: string
    course: string
}